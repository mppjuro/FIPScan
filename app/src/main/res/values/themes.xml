<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.FIPscan" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="windowActionBar">true</item>
        <item name="windowNoTitle">true</item>
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/teal_700</item>
        <item name="colorPrimaryVariant">@color/teal_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_700</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/white</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- Custom attributes -->
        <item name="switchTextColor">@color/dark_gray</item>
        <!-- Ensure good contrast for text -->
        <item name="android:textColorPrimary">@color/black</item>
        <item name="android:textColorSecondary">@color/dark_gray</item>
        <!-- Background colors -->
        <item name="android:colorBackground">@color/white</item>
        <item name="android:colorBackgroundFloating">#F8F8F8</item>
        <item name="android:dividerHorizontal">@color/light_grayE0</item>
    </style>

    <style name="Theme.FIPscan.Dark" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="switchTextColor">@color/light_gray</item>
        <!-- Dark theme specific colors -->
        <item name="android:textColorPrimary">@color/white</item>
        <item name="android:textColorSecondary">@color/light_gray</item>
        <item name="android:colorBackground">#121212</item>
        <item name="android:colorBackgroundFloating">#1E1E1E</item>
    </style>
</resources>